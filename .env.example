# Example environment variables for this project
# Copy to .env.development and/or .env.production and fill real values.

# Backend API base URL used by axios client (no trailing slash)
REACT_APP_BASE_URL=http://localhost:8000

# Public-facing base (optional). Used as fallback if REACT_APP_BASE_URL is not set.
REACT_APP_BASE_URL_PUBLIC=https://public.example.com
